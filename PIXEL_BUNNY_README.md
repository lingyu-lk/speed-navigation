# 像素兔子跟随效果 🐰

一个可爱的像素风格兔子跟随鼠标的交互效果。

## 功能特性

✅ **智能跟随** - 兔子会朝着鼠标方向旋转并平滑移动
✅ **状态切换** - 移动时跑动，静止时坐下休息
✅ **边界检测** - 自动限制在页面范围内，不会越界
✅ **性能优化** - 使用 requestAnimationFrame 实现流畅动画
✅ **响应式支持** - 适配不同屏幕尺寸
✅ **像素风格** - 使用 CSS image-rendering 保持像素风格

## 文件结构

```
js/pixel-bunny.js      # 核心逻辑代码
css/pixel-bunny.css    # 样式和动画
```

## 配置参数

在 `js/pixel-bunny.js` 中可以调整的参数：

```javascript
this.config = {
    speed: 0.08,              // 移动速度（0-1，值越大越快）
    rotationSpeed: 0.15,      // 旋转速度
    stopDelay: 800,           // 鼠标静止多久后坐下（毫秒）
    boundary: 20,             // 距离边界的安全距离（像素）
    arrivalThreshold: 5       // 到达目标点的阈值（像素）
};
```

## 自定义兔子图片

### 方案1：使用 Emoji（默认）

当前使用 emoji 作为兔子：
- 🐰 跑动状态
- 🐇 坐下状态

### 方案2：使用自定义图片

1. 准备两张透明背景的像素兔子图片：
   - `bunny-running.png` - 跑动动画（建议使用 sprite sheet）
   - `bunny-sitting.png` - 坐下动画

2. 在 `css/pixel-bunny.css` 中取消注释以下代码：

```css
.bunny-sprite.running {
    background-image: url('../assets/bunny-running.png');
    animation: bunnyRunSprite 0.4s steps(4) infinite;
}

.pixel-bunny.sitting .bunny-sprite {
    background-image: url('../assets/bunny-sitting.png');
    animation: bunnySitSprite 0.6s steps(3) 1;
}
```

3. 调整帧数：
   - `steps(4)` 表示跑动动画有4帧
   - `steps(3)` 表示坐下动画有3帧
   - `-192px` = 48px × 4帧（根据实际帧数调整）

## Sprite Sheet 格式

推荐的精灵图格式：

```
跑动动画（192×48，4帧）：
┌────┬────┬────┬────┐
│ 帧1│ 帧2│ 帧3│ 帧4│
└────┴────┴────┴────┘

坐下动画（144×48，3帧）：
┌────┬────┬────┐
│ 帧1│ 帧2│ 帧3│
└────┴────┴────┘
```

## 控制方法

### 启用/禁用

```javascript
// 禁用兔子
if (window.pixelBunny) {
    window.pixelBunny.destroy();
}

// 重新启用
window.pixelBunny = new PixelBunny();
```

### 调整参数

```javascript
// 修改移动速度
window.pixelBunny.config.speed = 0.15;

// 修改静止延迟
window.pixelBunny.config.stopDelay = 1000;
```

## 移动端适配

默认在移动端会缩小尺寸。如果想在移动端隐藏，可以在 `css/pixel-bunny.css` 中取消注释：

```css
@media (max-width: 768px) {
    .pixel-bunny {
        display: none;
    }
}
```

## 性能优化

- ✅ 使用 `requestAnimationFrame` 而非 `setInterval`
- ✅ 使用 `will-change` 提示浏览器优化
- ✅ 使用 `pointer-events: none` 避免影响页面交互
- ✅ 延迟加载（页面加载1秒后才创建）
- ✅ 使用 `transform` 而非 `left/top` 实现移动

## 浏览器兼容性

- ✅ Chrome/Edge 90+
- ✅ Firefox 88+
- ✅ Safari 14+
- ⚠️ IE 不支持

## 故障排除

### 兔子不显示
- 检查浏览器控制台是否有错误
- 确认 CSS 和 JS 文件正确加载
- 查看是否被 z-index 覆盖

### 移动卡顿
- 降低 `speed` 参数
- 增加 `arrivalThreshold` 值
- 检查是否有其他性能密集型脚本

### 跳出边界
- 增加 `boundary` 参数值
- 检查页面是否有固定定位元素影响

## 推荐素材网站

免费像素兔子素材：
- [OpenGameArt](https://opengameart.org/)
- [itch.io](https://itch.io/game-assets/free)
- [Kenney.nl](https://kenney.nl/)
- [Pixilart](https://www.pixilart.com/)

## 开发者

由 Claude Code 生成 🤖

## 许可证

MIT License
